---
title: Work1 简单的弹窗
createTime: 2025/10/04 18:34:50
permalink: /LearningRoute/5tmkq85p/
---
> 该练习考察简单的css定位与js的dom操作

## 需求描述

使用html，css，js来实现一个简单的弹窗：

* 初始弹窗不可视，点击按钮后触发弹窗

* 要求触发弹窗后全局页面有遮罩层

* 点击弹窗左上角的x号或点击弹窗外的部分可以关闭弹窗

## 需求分析

该弹窗的核心需求即实现弹窗可视状态的切换

初始不可视：`display:none`

点击（click）按钮后触发弹窗即为按钮绑定事件监听器，当click事件发生后修改弹窗的display不为none即可

遮罩层同理

## 需求实现

首先确定html布局

```html
<body>
    <button id="openModal">打开弹窗</button>

    <!-- 遮罩层 -->
    <div id="overlay" class="overlay"></div>

    <!-- 弹窗 -->
    <div id="modal" class="modal">
      <div class="modal-content">
        <span id="closeModal" class="close">&times;</span>
        <h2>弹窗标题</h2>
        <p>这是一个弹窗的内容。</p>
      </div>
    </div>
  </body>
```

为遮罩层与弹窗确定基础样式

```css
.overlay {
  display: none; /* 初始隐藏 */
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 999; /* 使其在所有元素之上 */
}

/* 弹窗样式 */
.modal {
  display: none; /* 初始隐藏 */
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 300px;
  background-color: #fff;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  z-index: 1000; /* 遮罩层之上 */
}

/* 弹窗内容样式 */
.modal-content {
  text-align: center;
}

/* 关闭按钮样式 */
.close {
  position: absolute;
  top: 10px;
  left: 10px;
  cursor: pointer;
}
```

在js中操作dom元素

```javascript
"use strict";
const modal = document.getElementById("modal");
const overlay = document.getElementById("overlay");
const openModalBtn = document.getElementById("openModal");
const closeModalBtn = document.getElementById("closeModal");

// 打开弹窗和遮罩层
openModalBtn.addEventListener("click", () => {
  modal.style.display = "block";
  overlay.style.display = "block";
});

// 关闭弹窗和遮罩层
closeModalBtn.addEventListener("click", () => {
  modal.style.display = "none";
  overlay.style.display = "none";
});

// 点击遮罩层关闭弹窗
overlay.addEventListener("click", () => {
  modal.style.display = "none";
  overlay.style.display = "none";
});
```

## 贡献者

<div class="contributors-list" style="display: flex; gap: 20px; flex-wrap: wrap; margin-top: 20px;">
  <!-- 贡献者 1 -->
  <div style="text-align: center;">
    <img src="https://avatars.githubusercontent.com/u/145993470?v=4" alt="吴限" style="width: 80px; border-radius: 50%;" />
    <p style="margin-top: 8px;"><a href="https://github.com/zhuningyu3" target="_blank">吴限</a></p>
  </div>
</div>
